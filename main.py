import interface
import logger
import result
last_name, first_name, phone_number, comment = interface.get_user()
print(last_name, first_name, phone_number, comment)
# print(result.res(last_name, first_name, phone_number, comment))   #лишнее действие? Без него работает, запись идет в лог












    
  
 